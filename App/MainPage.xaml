<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.MainPage"
             BackgroundColor="{DynamicResource SecondaryColor}">

	<ScrollView>
		<StackLayout >

			<Editor BindingContext="{Binding Node}" Text="{Binding Text}"   />

			<Button Clicked="OnSaveClicked" Text="Save" />

			<ListView ItemsSource="{Binding Operations}"
              Margin="20">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<StackLayout Margin="20,0,0,0" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
								<StackLayout Orientation="Vertical" HorizontalOptions="Start">
									<Label Text="{Binding Type}"  VerticalOptions="Start" />
									<Label Text="{Binding Path}" VerticalOptions="Start" />
									<Label Text="{Binding Value}" VerticalOptions="End" />
									<Label Text="{Binding From}" VerticalOptions="End" />
								</StackLayout>
							</StackLayout>
						</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ScrollView>
</ContentPage>
